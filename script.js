const botao = document.querySelector("button");
botao.addEventListener("click",botaoClicado);
//cria um evento e click e chama a função botaoClicado
botao.addEventListener("click",botaoClicado);

//cria a função que fará com que o texto do numeero zero some + 1 a cada click
function botaoClicado() {
 let texto = botao, querySelection("spam");
  texto.textContent++;
}

//               ####legendas;
//querySelector; seleciona o primeiro elemento que corresponde a um seletor CSS especificado,
//adEventListener; adiciona um evento de click, prmitindo que você execute uma função quando o evento ocorre.
